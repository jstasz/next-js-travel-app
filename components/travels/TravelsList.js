function TravelList(props) {
  return (
    <ul>
      {/* {props.travels.map(travel =>
        <TravelItem
          key={travel.id}
          id={travel.id}
          image={travel.image}
          city={travel.place}
          placesToSee={travel.address}
        />
      )} */}
    </ul>
  );
}
